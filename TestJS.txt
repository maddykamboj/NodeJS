var map = new Map();
map.set("Item1", "Jest");
map.set("Item2", "Jetty")
for (let [key,value] of map){
    console.log(key+ "-"+value );
}


var map = new Map();
map.set("Item1", "Jest");
map.set("Item2", "Jetty")
for (let element of map){
    console.log(element);
}

var map = new Map();
map.set("Item1", "Jest");
map.set("Item2", "Jetty")
for (let key of map.keys()){
    console.log(key);
}